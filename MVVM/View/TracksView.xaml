<UserControl x:Class="dotnet_YouTubeAPI.MVVM.View.TracksView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dotnet_YouTubeAPI.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <DockPanel>
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Image Grid.ColumnSpan="2" Height="100" Source="/Static/Tracks.jpg" Opacity=".3" Stretch="UniformToFill"></Image>
            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="8">
                <TextBlock Text="Favourite Tracks" Foreground="Black" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="8" HorizontalAlignment="Left">
                <TextBox Width="250" Height="40" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="5" Style="{StaticResource SearchBar}" TextChanged="TextBox_TextChanged"/>
            </StackPanel>
            
        </Grid>
        <StackPanel Background="LightGray" ScrollViewer.CanContentScroll="False">
          
        </StackPanel>
    </DockPanel>
</UserControl>
